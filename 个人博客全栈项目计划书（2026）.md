# 个人博客全栈项目计划书（2026）- Supabase BaaS 版

## 📌 项目概述

基于 Next.js 16 + TypeScript + Tailwind CSS + MDX + Supabase + FastAPI 构建的全栈个人博客系统，目标是在三周内完成从零到部署的完整开发。项目以技术写作与知识分享为核心场景，数据、认证、权限校验统一采用 Supabase BaaS 能力：Supabase PostgreSQL 存储业务数据，Supabase Auth 管理用户注册/登录/会话，RLS Policy 控制数据访问权限。FastAPI 只负责业务 API 编排、Pydantic 数据校验与 Swagger 文档，不再自建 JWT、密码哈希或 Token 黑名单机制。

## 🎯 项目目标

| 维度   | 目标                                      |
| ---- | --------------------------------------- |
| 内容管理 | Supabase PostgreSQL 存储文章，支持在线 CRUD 操作 |
| 认证体系 | Supabase Auth 实现注册、登录、会话与用户身份校验 |
| 权限控制 | Supabase RLS Policy 控制公开、登录用户、作者、管理员权限 |
| 用户体验 | 首屏加载 < 1.5s，Lighthouse 评分 > 90 |
| 功能覆盖 | 博客列表、全文搜索、暗色模式、评论互动、用户认证 |
| 部署上线 | Next.js 部署 Vercel，FastAPI 部署 Render/VPS，Supabase 托管数据与认证 |
| 开发效率 | 100% TypeScript 类型覆盖，前后端分离，API 文档自动生成 |

## 🛠️ 技术架构

### 前端层（Next.js 16）

- 框架：Next.js 16 (App Router)
- 语言：TypeScript 严格模式
- 样式：Tailwind CSS v4 + shadcn/ui
- 内容渲染：`next-mdx-remote`（从数据库动态加载 MDX）
- 状态管理：React Context + SWR/TanStack Query

### 后端层（FastAPI）

- 框架：FastAPI 0.110+
- 语言：Python 3.11+
- 认证：使用 Supabase Auth SDK 校验 Supabase 会话，不自建 JWT
- 数据访问：通过 Supabase Python SDK / PostgREST 调用 Supabase BaaS 能力
- 数据校验：Pydantic v2
- API 文档：自动生成 Swagger UI + ReDoc

### 数据库层（Supabase BaaS / PostgreSQL）

- BaaS 平台：Supabase
- 数据库：Supabase PostgreSQL 15+
- 认证：Supabase Auth 管理用户、邮箱登录、会话刷新与访问 Token
- 用户资料：业务侧只维护 `profiles` 表，主键关联 Supabase `auth.users.id`
- 数据表：profiles、posts、tags、post_tags、comments
- 权限校验：Supabase RLS Policy + `auth.uid()`，不在 FastAPI 自建 JWT 校验逻辑
- 扩展支持：pgvector（为后续 AI 功能预留）

## 📅 三周开发时间线

### 第一周：前端基础架构

#### Day 1（周一）—— Next.js 项目初始化

**上午**

- 使用 `create-next-app` 初始化项目，启用 TypeScript、ESLint、Tailwind CSS
- 配置 `next.config.mjs`，设置路径别名 `@/*`
- 规划前端项目目录结构

**下午**

- 安装并配置核心依赖：
  - 样式：`tailwindcss` + `shadcn/ui`（Button、Card、Dialog、Dropdown Menu）
  - 工具库：`clsx`、`tailwind-merge`、`date-fns`
  - 数据获取：`@tanstack/react-query`、`axios`
- 配置 `globals.css` 中的 CSS 变量，定义亮色/暗色主题色板
- 搭建基础 Layout（Header + Main + Footer）

**产出**：可运行的项目骨架，带基础布局和主题切换能力

#### Day 2（周二）—— 前端页面搭建

**上午**

- 创建首页 `app/page.tsx`（Hero 区域 + 最新文章列表）
- 创建文章列表页 `app/blog/page.tsx`
- 创建文章详情页 `app/blog/[slug]/page.tsx`（骨架，暂不渲染 MDX）
- 创建标签聚合页 `app/tags/page.tsx`

**下午**

- 完善前端组件：
  - `PostCard.tsx`：文章卡片组件
  - `PostList.tsx`：文章列表组件
  - `TagCloud.tsx`：标签云组件
  - `ThemeToggle.tsx`：暗色模式切换
- 配置 `next-themes` 实现暗色模式持久化
- 实现响应式布局适配（移动端/平板/桌面）

**产出**：所有前端页面骨架搭建完成，UI 组件就绪

### 第二周：后端与数据层

#### Day 3（周三）—— Supabase BaaS 数据模型与 FastAPI 初始化

**上午**

- 在 Supabase 创建项目，启用 Supabase Auth 邮箱登录
- 配置 Supabase Project URL、Anon Key、Service Role Key、数据库连接信息
- 使用 Supabase SQL Editor 设计业务表结构，用户身份直接关联 Supabase `auth.users`：

```sql
-- 用户资料表：认证用户由 Supabase auth.users 管理
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  username TEXT UNIQUE NOT NULL,
  avatar_url TEXT,
  bio TEXT,
  role TEXT NOT NULL DEFAULT 'user' CHECK (role IN ('user', 'author', 'admin')),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 文章表
CREATE TABLE posts (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  title TEXT NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  content TEXT NOT NULL,
  description TEXT,
  cover_image TEXT,
  author_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  reading_time INT DEFAULT 0,
  is_published BOOLEAN DEFAULT FALSE,
  view_count INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 标签表
CREATE TABLE tags (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name TEXT UNIQUE NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 文章-标签关联表
CREATE TABLE post_tags (
  post_id UUID REFERENCES posts(id) ON DELETE CASCADE,
  tag_id UUID REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (post_id, tag_id)
);

-- 评论表（可选）
CREATE TABLE comments (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  post_id UUID REFERENCES posts(id) ON DELETE CASCADE,
  user_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  parent_id UUID REFERENCES comments(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 创建索引优化查询
CREATE INDEX idx_posts_slug ON posts(slug);
CREATE INDEX idx_posts_author_id ON posts(author_id);
CREATE INDEX idx_posts_created_at ON posts(created_at DESC);
CREATE INDEX idx_posts_is_published ON posts(is_published);
CREATE UNIQUE INDEX idx_tags_slug ON tags(slug);
```

- 启用 RLS 并规划权限策略：
  - 未登录用户：只能读取已发布文章、标签、公开资料
  - 登录用户：可创建文章、评论，读取自己的草稿
  - 作者：只能编辑/删除自己的文章
  - 管理员：可管理文章、标签和用户资料

**下午**

- 初始化 FastAPI 项目，创建目录结构：

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI 入口
│   ├── config.py            # 环境变量配置
│   ├── supabase.py          # Supabase Client 初始化
│   ├── schemas.py           # Pydantic 模型
│   ├── auth/
│   │   ├── __init__.py
│   │   └── dependencies.py  # Supabase Auth 认证依赖注入
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── posts.py         # 文章路由
│   │   ├── tags.py          # 标签路由
│   │   └── users.py         # 用户资料路由
│   └── services/
│       ├── posts.py         # Supabase 数据访问封装
│       └── tags.py
├── requirements.txt
└── .env
```

- 配置 FastAPI CORS 允许 Next.js 前端调用
- 配置环境变量（Supabase URL、Anon Key、Service Role Key、CORS 白名单）

**产出**：Supabase BaaS 数据模型、Auth、RLS 规划完成，FastAPI 项目骨架搭建完成

#### Day 4（周四）—— Supabase Auth 集成与权限校验实现

**上午**

- 初始化 Supabase Python Client
- 实现 Pydantic Schema 定义（请求/响应模型）
- 实现 Supabase Auth 依赖注入：
  - `get_current_user()`：从 `Authorization: Bearer` 头提取 Supabase Access Token，并调用 Supabase Auth 校验
  - `get_current_profile()`：读取当前用户的 `profiles` 资料
  - `require_author()`：校验作者或管理员权限
  - `require_admin()`：校验管理员权限
- 配置受保护路由统一使用 Supabase Auth 依赖，不手写 Token 签发、解析、刷新逻辑

**下午**

- 实现用户资料路由：
  - `GET /api/users/me`：获取当前用户资料（需 Supabase 登录态）
  - `PUT /api/users/me`：更新当前用户资料（需 Supabase 登录态）
- 前端使用 `@supabase/supabase-js` 实现注册、登录、登出、会话恢复
- 后端只接收并校验 Supabase 已签发的 Access Token
- 在 Supabase 控制台验证 Auth 用户、profiles 关联、RLS 策略生效

**产出**：Supabase Auth 登录态打通，后端保护路由可识别当前用户，项目不包含自建 JWT 功能

#### Day 5（周五）—— 文章与标签 CRUD 接口实现

**上午**

- 实现文章 CRUD 路由：
  - `GET /api/posts`：获取文章列表（支持分页、排序、搜索）
  - `GET /api/posts/{slug}`：获取单篇文章
  - `POST /api/posts`：创建文章（需认证）
  - `PUT /api/posts/{id}`：更新文章（需认证 + 权限检查）
  - `DELETE /api/posts/{id}`：删除文章（需认证 + 权限检查）
- 实现标签 CRUD 路由：
  - `GET /api/tags`：获取所有标签
  - `POST /api/tags`：创建标签（需认证）
  - `DELETE /api/tags/{id}`：删除标签（需认证）

**下午**

- 实现文章-标签关联操作
- 实现 MDX 内容存储与读取（数据库中直接存储 MDX 全文）
- 实现文章搜索功能（使用 PostgreSQL 全文搜索）
- 实现前端 API 客户端类型生成（通过 OpenAPI）
- 编写接口文档（FastAPI 自动生成 Swagger UI）

**产出**：完整的 CRUD 接口，文章与标签关联操作可用

### 第三周：前后端联调与部署

#### Day 6（周一）—— 前端数据层与 Supabase Auth 集成

**上午**

- 配置前端 API 客户端（axios + React Query）
- 配置 Supabase Browser Client（`@supabase/supabase-js`）
- 实现文章列表数据获取和展示
- 实现文章详情页 MDX 渲染（使用 `next-mdx-remote`）
- 实现文章创建/编辑表单（使用 shadcn/ui Form 组件）
- 实现文章删除功能

**下午**

- 使用 Supabase Auth 实现用户认证状态管理
- 实现登录/注册页面和表单，调用 Supabase Auth API
- 使用 Supabase Session 实现登录状态恢复，不手动保存自建 Token
- 实现受保护路由（未登录重定向）
- 配置 API 请求拦截器：从 Supabase Session 读取 Access Token 并添加 `Authorization: Bearer` Header

**产出**：前端完整集成 Supabase Auth 与后端 API，认证流和文章 CRUD 可用

#### Day 7（周二）—— 全文搜索与标签系统

**上午**

- 实现客户端全文搜索：
  - 使用 `minisearch` 构建本地索引
  - 搜索范围覆盖标题、描述、正文内容
  - 搜索 UI 使用 shadcn/ui 的 Dialog + Command 组合（⌘K 风格）
- 搜索接口对接后端搜索 API（PostgreSQL 全文搜索）

**下午**

- 实现标签系统：
  - 标签聚合页 `/tags`：展示所有标签及对应文章数量
  - 标签筛选页 `/tags/[tag]`：按标签过滤文章列表
  - 文章卡片展示标签徽章
- 实现文章列表分页（每页 10 篇）
- 优化文章列表加载速度（SSR + 缓存）

**产出**：全文搜索和标签系统完整可用

#### Day 8（周三）—— 评论系统与 SEO

**上午**

- 集成 Giscus 评论系统：
  - 配置 GitHub Discussions 仓库
  - 评论主题跟随暗色模式切换
  - 通过环境变量配置仓库信息
- 实现文章阅读量统计（更新 `view_count` 字段）

**下午**

- 优化文章页 SEO：
  - 生成动态 `metadata`（title、description、openGraph）
  - 配置 `robots.txt` 和 `sitemap.xml`
  - 添加社交媒体 Meta 标签
- 实现 RSS Feed 生成（`app/feed.xml/route.ts`）
- 实现站点地图自动生成（`sitemap.ts`）

**产出**：评论区可用，SEO 基础优化完成

#### Day 9（周三）—— 性能优化与安全加固

**上午**

- 性能优化专项：
  - 使用 `next/dynamic` 懒加载评论组件和搜索组件
  - 图片使用 next/image + WebP 格式
  - 启用 SWC 压缩
  - 使用 `next/font` 加载 Google Fonts
- 配置前端缓存策略（Next.js ISR）

**下午**

- 安全加固：
  - 使用 Supabase Auth 控制会话、刷新 Token 与用户身份验证
  - 使用 Supabase RLS Policy 限制数据访问范围
  - 后端保护路由统一调用 Supabase Auth 校验当前用户
  - 配置 CORS 白名单
  - 敏感 Supabase Service Role Key 仅保存在后端环境变量
  - 启用 HTTPS（部署层配置）
- 集成监控工具：
  - `@vercel/analytics`
  - `@vercel/speed-insights`

**产出**：Lighthouse 评分 ≥ 90，Supabase Auth 与 RLS 安全策略生效

#### Day 10（周五）—— 部署上线与交付

**上午**

- 后端部署（Render.com / Railway / 自建 VPS）：
  - 配置环境变量
  - 设置自动部署
- 前端部署（Vercel）：
  - 关联 GitHub 仓库
  - 配置环境变量（API_BASE_URL、Giscus 配置等）
  - 绑定自定义域名

**下午**

- 最终交付测试：
  - 完整流程回归：首页 → 列表 → 详情 → 评论 → 搜索
  - 移动端响应式检查（375px ~ 1920px）
  - 暗色/亮色模式切换测试
  - CRUD 流程完整测试
  - Supabase Auth 认证流程完整测试
- 编写项目文档：
  - `README.md`（技术栈、本地运行指南）
  - API 文档（Swagger UI 自动生成）
  - 环境变量说明
- 交付项目源码 + 部署链接 + 测试报告

**产出**：线上可访问的个人博客，完整前后端分离架构

## 🔐 认证与权限设计

### Supabase Auth 认证流程

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   用户      │      │   Frontend  │      │   Backend   │      │  Supabase   │
│  (浏览器)   │      │  (Next.js)  │      │  (FastAPI)  │      │ Auth + DB   │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │                    │
       │ 1. 登录/注册        │                    │                    │
       │───────────────────>│ 2. supabase.auth   │                    │
       │                    │────────────────────────────────────────>│
       │                    │ 3. 返回 Session    │                    │
       │<───────────────────│<────────────────────────────────────────│
       │                    │                    │                    │
       │ 4. 请求受保护 API   │                    │                    │
       │ (Bearer Access Token)                  │                    │
       │───────────────────>│ 5. 转发 Token      │                    │
       │                    │───────────────────>│ 6. Supabase Auth 校验│
       │                    │                    │───────────────────>│
       │                    │                    │ 7. 返回用户身份      │
       │                    │                    │<───────────────────│
       │                    │                    │ 8. 按 RLS/角色访问数据│
       │                    │                    │───────────────────>│
       │ 9. 返回业务数据     │<───────────────────│<───────────────────│
```

### 权限控制矩阵

| 操作      | 未登录 | 普通用户 | 文章作者 | 管理员 |
| ------- | --- | ---- | ---- | --- |
| 查看文章列表  | ✅   | ✅    | ✅    | ✅   |
| 查看文章详情  | ✅   | ✅    | ✅    | ✅   |
| 创建文章    | ❌   | ✅    | ✅    | ✅   |
| 编辑自己的文章 | ❌   | ❌    | ✅    | ✅   |
| 编辑他人文章  | ❌   | ❌    | ❌    | ✅   |
| 删除自己的文章 | ❌   | ❌    | ✅    | ✅   |
| 删除他人文章  | ❌   | ❌    | ❌    | ✅   |
| 管理标签    | ❌   | ❌    | ❌    | ✅   |
| 管理用户    | ❌   | ❌    | ❌    | ✅   |

### 权限实现方式

- 用户登录、注册、登出、会话刷新全部使用 Supabase Auth。
- 业务表开启 RLS，通过 `auth.uid()` 判断当前 Supabase 用户。
- 作者权限通过 `posts.author_id = auth.uid()` 判断。
- 管理员权限通过 `profiles.role = 'admin'` 判断。
- FastAPI 保护路由只调用 Supabase Auth 获取当前用户，不签发、不解析自建 JWT。

## 📁 更新后的目录结构

```
personal-blog/
├── frontend/                          # Next.js 前端
│   ├── app/
│   │   ├── (home)/page.tsx
│   │   ├── blog/
│   │   │   ├── page.tsx
│   │   │   └── [slug]/
│   │   │       └── page.tsx
│   │   ├── tags/
│   │   │   ├── page.tsx
│   │   │   └── [tag]/
│   │   │       └── page.tsx
│   │   ├── auth/
│   │   │   ├── login/page.tsx
│   │   │   └── register/page.tsx
│   │   ├── dashboard/                 # 管理后台
│   │   │   ├── page.tsx               # 文章管理
│   │   │   └── posts/
│   │   │       └── new/page.tsx       # 新建文章
│   │   ├── feed.xml/route.ts
│   │   ├── sitemap.ts
│   │   ├── layout.tsx
│   │   ├── globals.css
│   │   └── providers.tsx              # Context Providers
│   ├── components/
│   │   ├── ui/                        # shadcn/ui
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── ThemeToggle.tsx
│   │   ├── blog/
│   │   │   ├── PostCard.tsx
│   │   │   ├── PostList.tsx
│   │   │   ├── PostDetail.tsx
│   │   │   └── TOC.tsx
│   │   ├── search/
│   │   │   └── SearchDialog.tsx
│   │   └── auth/
│   │       ├── LoginForm.tsx
│   │       └── RegisterForm.tsx
│   ├── lib/
│   │   ├── api/                       # API 客户端
│   │   │   ├── client.ts
│   │   │   ├── posts.ts
│   │   │   └── auth.ts
│   │   ├── hooks/                     # React Query Hooks
│   │   │   ├── usePosts.ts
│   │   │   └── useAuth.ts
│   │   └── utils.ts
│   ├── types/
│   │   └── index.ts
│   ├── .env.local
│   └── package.json
│
├── backend/                           # FastAPI 后端
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py
│   │   ├── config.py                  # 环境变量配置
│   │   ├── supabase.py                # Supabase Client 初始化
│   │   ├── schemas.py                 # Pydantic 模型
│   │   ├── auth/
│   │   │   ├── __init__.py
│   │   │   └── dependencies.py        # 认证依赖
│   │   └── routes/
│   │       ├── __init__.py
│   │       ├── auth.py
│   │       ├── posts.py
│   │       ├── tags.py
│   │       └── users.py
│   ├── requirements.txt
│   ├── .env
│
└── README.md
```

## 🔧 环境变量配置

### 后端 `.env`

```env

# Supabase 配置
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# CORS 配置
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# 环境
ENVIRONMENT=development
```

### 前端 `.env.local`

```env

# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# API 配置
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Giscus 配置
NEXT_PUBLIC_GISCUS_REPO=username/repo
NEXT_PUBLIC_GISCUS_REPO_ID=R_kgDO...
NEXT_PUBLIC_GISCUS_CATEGORY_ID=DIC_kwDO...
```

## 🚀 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户访问                              │
│                   (blog.yourname.com)                       │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    Vercel (前端托管)                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Next.js 16 (App Router)                            │  │
│  │  - 静态页面 (SSG)                                    │  │
│  │  - 动态渲染 (SSR)                                    │  │
│  │  - Supabase Auth Client                             │  │
│  │  - API 代理 (BFF)                                    │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼  API 请求（携带 Supabase Access Token）
┌─────────────────────────────────────────────────────────────┐
│                  Render.com / Railway (后端托管)            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  FastAPI + Uvicorn                                  │  │
│  │  - Pydantic 数据校验                                 │  │
│  │  - Supabase Auth 用户校验                            │  │
│  │  - CRUD 业务编排                                     │  │
│  │  - 自动 API 文档 (Swagger)                           │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼  Supabase SDK / PostgREST
┌─────────────────────────────────────────────────────────────┐
│                    Supabase BaaS                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Auth                                                │  │
│  │  - 用户注册/登录/登出                                 │  │
│  │  - Session 与 Access Token 管理                       │  │
│  │                                                       │  │
│  │  PostgreSQL 15+                                      │  │
│  │  - profiles / posts / tags / comments                │  │
│  │  - RLS 权限控制                                      │  │
│  │  - 全文搜索                                          │  │
│  │  - pgvector (AI 扩展预留)                            │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 📋 交付物清单

- [ ] 完整的 Next.js 前端源码（GitHub 仓库）
- [ ] 完整的 FastAPI 后端源码（GitHub 仓库）
- [ ] 线上生产环境可访问地址（Vercel + Render/Railway）
- [ ] Swagger API 文档（自动生成）
- [ ] 项目 README 文档（含启动指南和环境变量说明）
- [ ] Supabase BaaS 数据模型与 RLS Policy 设计
- [ ] 10+ 篇示例博文（含标签和封面图）
- [ ] 全功能测试报告（Lighthouse、功能清单、API 测试）

## 📈 后续迭代路线图

| 阶段   | 功能                             | 预计时间 |
| ---- | ------------------------------ | ---- |
| v1.1 | AI 摘要（使用 FastAPI + OpenAI SDK） | 1 周  |
| v1.2 | 相关文章推荐（pgvector 向量检索）          | 3 天  |
| v1.3 | 浏览量统计 + 文章热榜                   | 3 天  |
| v1.4 | 邮件订阅（Newsletter）               | 3 天  |
| v2.0 | AI 问答助手（RAG 架构）                | 2 周  |

## 🎯 关键学习目标

通过本项目你将掌握：

| 领域      | 技能                                             |
| ------- | ---------------------------------------------- |
| **数据库** | Supabase BaaS、PostgreSQL 表设计、Supabase Auth、RLS 策略、全文搜索         |
| **后端**  | FastAPI 框架、Supabase SDK、Supabase Auth 认证、权限控制          |
| **前端**  | Next.js 16 App Router、数据预取、React Query 状态管理    |
| **部署**  | Vercel 前端部署、Python 后端部署、环境变量管理                 |
| **工具链** | 类型安全 API（OpenAPI 生成）、Swagger 文档、Supabase CLI |
