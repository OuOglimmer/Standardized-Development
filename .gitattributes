# Keep repository text files compatible with Linux deployments.
* text=auto eol=lf

# Windows command scripts use native line endings.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Unix shell scripts must use LF.
*.sh text eol=lf
